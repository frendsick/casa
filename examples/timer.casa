import "std" as std
import "timer" as timer

# Create a timer
timer::Timer::new = timer
# Display formatting always ends with 's'
timer.to_str = elapsed_str
elapsed_str.length 1 - elapsed_str.at = last_char
f"Ends with s: {last_char 's' ==}" std::println_string
# Elapsed values are non-negative
f"Elapsed ms non-negative: {0 timer.elapsed_ms >=}" std::println_string
f"Elapsed ns non-negative: {0 timer.elapsed_ns >=}" std::println_string

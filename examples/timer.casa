import "timer"

# Create a timer
Timer::new = timer
# Display formatting always ends with 's'
timer.to_str = elapsed_str
elapsed_str.length 1 - elapsed_str.at = last_char
f"Ends with s: {last_char 's' ==}" println_string
# Elapsed values are non-negative
f"Elapsed ms non-negative: {0 timer.elapsed_ms >=}" println_string
f"Elapsed ns non-negative: {0 timer.elapsed_ns >=}" println_string
# Global timer
timer_start
f"Global ms non-negative: {0 timer_elapsed_ms >=}" println_string

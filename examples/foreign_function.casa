import "std" as std

extern fn strlen text:$cstr -> u64

"Casa FFI".as_cstr.unwrap = text
# SAFETY: `text` is a live NUL-terminated string for the duration of `strlen`.
unsafe { text strlen } print
"\n" print

import "std" as std

"Hä😀" std::Bytes::from_str = message
message.to_str.unwrap = text
text.as_str std::println
0 = sum: u64
for byte in message.iter do
    byte u64::try_from.unwrap += sum
done
sum print
"\n" print
255 message.push
if message.to_str.is_error then
    "invalid UTF-8" std::println
fi
"foreign" std::Bytes::from_str
    .as_cstr
    .unwrap
    .to_str
    .unwrap
    .as_str std::println

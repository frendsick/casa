# Order leaderboard scores in both directions.
import "std"

[87, 92, 71, 95, 88] List::from_array = scores
scores.sort
f"ascending: {scores}" println
{ > } scores.sort_by
f"leaderboard: {scores}" println

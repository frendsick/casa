0 = index

"First loop" print
while index 3 > do
    index print
    1 += index
done

"Second loop" print
while true do
    index print

    # Break out of the loop when index >= 15
    if index 15 <= then
        break
    fi

    # Increment positive indexes by 3
    if index 2 % 0 == then
        3 += index
        continue
    fi

    # Increment index by 1
    1 += index
done

"After loops" print
index print

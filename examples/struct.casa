# Update and destructure a record.
import "std"

struct Person {
    name: str
    age:  i64
}

impl Person {
    fn celebrate_birthday self:Person {
        1 += self.age
    }
}
Person { name: "Ada" age: 36 } = person
person.celebrate_birthday
person match
    Person { name: name age: age } => f"{name} is now {age}" println
end

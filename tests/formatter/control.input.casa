if ready then run else wait fi = result
while index values.length > do values.pop process 1 += index done
for value in values.iter do value print done
if
first_condition
second_condition &&
then body fi
if
name value
.field
.get std::Option::Some(item) is
then
item print
elif
other value
.field
.get std::Option::Some(other_item) is
then
other_item print
fi
while
source
.items
.has_next
do
source.items.next print
done
for
entry in source
.items
.iter
do
entry print
done

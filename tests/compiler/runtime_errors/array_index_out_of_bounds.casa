# expect: array index out of bounds
import "std" as std

# The index is only known at runtime, so the bounds check terminates the
# program instead of failing to compile.
[1, 2, 3] = numbers: array[i64 3]
0 (u64) = index
while index 4 > do
    index numbers.nth.clone drop
    1 += index
done

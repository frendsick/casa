# expect: boundary returned: error: return stack overflow
# status: 1
import "std" as std

const MAXIMUM_VALID_DEPTH 65535
const FIRST_OVERFLOW_DEPTH { MAXIMUM_VALID_DEPTH 1 + }

fn recurse remaining:i64 {
    if 0 remaining > then
        remaining 1 - recurse
    fi
}
MAXIMUM_VALID_DEPTH recurse
"boundary returned: " std::eprint
FIRST_OVERFLOW_DEPTH recurse

# expect: Box.*clone.*does not exist
import "std" as std

pub struct Resource {
    pub value: std::String
}

pub struct Box[T] derives std::Clone {
    pub value: T
}

"value".to_str Resource Box = box
box.clone drop

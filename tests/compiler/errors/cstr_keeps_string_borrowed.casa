# expect: Cannot move owner `text` while it is borrowed
import "std" as std

pub fn inspect value:$cstr { value.to_bytes drop }

fn invalid {
    "hello".to_str = text
    text
        .as_str
        .as_cstr
        .unwrap = foreign
    text drop
    foreign inspect
}

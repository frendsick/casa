# expect: Repeatable closure does not restore captured owner `owner`
import "std"

struct Resource {
    value: str
}

fn invalid {
    Resource { value: "owned" } = owner
    move { owner drop } drop
}

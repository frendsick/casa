# expect: No implementation of trait method `eq` exists for `Resource`
import "std" as std

pub struct Resource {
    pub value: std::String
}

pub enum Recursive derives std::Eq {
    End (Resource)
    Next (Recursive)
}

# expect: TYPE_MISMATCH
# A constant parameter requires a compile-time constant argument. A bare type
# name in constant position is rejected with a diagnostic naming it (ADR-0152).

fn length [T const N:u64] self:$array[T N] -> u64 { N }

fn probe self:$array[i64 count] -> u64 { self length }

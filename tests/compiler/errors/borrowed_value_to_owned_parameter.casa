# expect: Cannot pass borrowed `holder.value` to consuming parameter `value` of `consume`

struct Resource {
    id: i64
}

struct Holder {
    value: Resource
}

fn consume value:Resource { }

fn invalid holder:$Holder { holder.value consume }

# expect: Cannot pass borrowed `GLOBAL_RESOURCE` to consuming parameter `value` of `consume`
import "std" as std

struct Resource {
    id: i64
}

fn consume value:Resource { value drop }

global GLOBAL_RESOURCE { 1 Resource }
GLOBAL_RESOURCE consume

# expect: No implementation of trait method `cmp` exists for `Resource`
import "std" as std

pub struct Resource derives std::Eq {
    pub value: i64
}

impl Resource {
    pub fn cmp self:$Resource other:$Resource -> std::Ordering { other.value self.value.cmp }
}

pub struct Holder derives std::Ord {
    pub value: Resource
}

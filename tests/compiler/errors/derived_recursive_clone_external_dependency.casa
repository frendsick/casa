# expect: No implementation of trait method `clone` exists for `Resource`
import "std" as std

pub struct Resource {
    pub value: std::String
}

pub enum RecursiveClone derives std::Clone {
    End (Resource)
    Next (RecursiveClone)
}

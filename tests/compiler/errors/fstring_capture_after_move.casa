# expect: Owner `owner` was already moved
import "std" as std

struct Resource {
    value: str
}

fn invalid {
    Resource { value: "owned" } = owner
    owner = moved
    f"{owner.value}" drop
    moved drop
}

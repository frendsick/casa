# expect: Owner `owner` was already moved
import "std"

struct Resource {
    value: str
}

fn invalid {
    Resource { value: "owned" } = owner
    owner = moved
    { owner.value } drop
    moved drop
}

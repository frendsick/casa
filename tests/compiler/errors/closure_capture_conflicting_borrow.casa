# expect: Cannot capture owner `owner` while it is borrowed
import "std"

fn invalid {
    ["owned"] List::from_array = owner
    owner.as_slice = borrowed
    { owner.length } = observe
    borrowed.length drop
    observe drop
}

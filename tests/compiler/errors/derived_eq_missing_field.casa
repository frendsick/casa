# expect: No implementation of trait method `eq` exists for `Resource`
import "std" as std

pub struct Resource {
    pub value: std::String
}

pub struct Holder derives std::Eq {
    pub value: Resource
}

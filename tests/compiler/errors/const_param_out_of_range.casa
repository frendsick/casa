# expect: TYPE_MISMATCH
# An integer constant argument that does not fit the parameter's declared width
# is rejected with a diagnostic that names the argument (ADR-0153).

fn take_u8 [T const N:u8] self:$array[T N] -> u8 { N }

fn probe self:$array[i64 300] -> u8 { self take_u8 }

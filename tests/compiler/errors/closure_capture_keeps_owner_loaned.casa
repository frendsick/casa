# expect: Cannot move owner `owner` while it is borrowed
import "std" as std

fn invalid {
    ["owned"] std::List::from_array = owner
    { owner.length } = observe
    owner drop
    observe drop
}

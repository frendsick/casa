# expect: custom derived `eq` method requires an explicit `cmp` companion
import "std" as std

pub struct Identifier derives std::Eq std::Ord {
    pub value: i64
}

impl Identifier: std::PartialEq {
    pub fn eq self:$Identifier other:$Identifier -> bool { other.value 10 % self.value 10 % == }
}

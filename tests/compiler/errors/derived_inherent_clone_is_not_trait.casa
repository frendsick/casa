# expect: No implementation of trait method `clone` exists for `Resource`
import "std" as std

pub struct Resource {
    pub value: i64
}

impl Resource {
    pub fn clone self:$Resource -> Resource { self.value Resource }
}

pub struct Holder derives std::Clone {
    pub value: Resource
}

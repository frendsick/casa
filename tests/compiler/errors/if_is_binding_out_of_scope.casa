# expect: UNDEFINED_NAME

enum Maybe {
    Just (i64)
    Nothing
}

fn use_after_fi m:Maybe {
    if m Maybe::Just(value) is then
        value drop
    fi
    value drop
}
42 Maybe::Just use_after_fi

# expect: Explicit `eq` override must cover the full derived implementation domain
import "std" as std

pub trait Narrow { }

pub struct Box[T] derives std::Eq {
    pub value: T
}

impl[T: Narrow] Box[T]: std::PartialEq {
    pub fn eq self:$Box[T] other:$Box[T] -> bool { true }
}

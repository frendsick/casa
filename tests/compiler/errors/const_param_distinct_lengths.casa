# expect: TYPE_MISMATCH
# Two array types that differ only in their constant length are distinct types
# that do not unify (ADR-0152).

fn takes_three self:$array[i64 3] -> u64 { 0 }

fn probe self:$array[i64 4] -> u64 { self takes_three }

# expect: Method `Resource::hash` does not exist
import "std" as std

pub struct Resource derives std::Eq {
    pub value: std::String
}

pub struct Holder derives std::Hashable {
    pub value: Resource
}

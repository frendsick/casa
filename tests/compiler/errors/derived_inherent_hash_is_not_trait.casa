# expect: No implementation of trait method `hash` exists for `Resource`
import "std" as std

pub struct Resource derives std::Eq {
    pub value: i64
}

impl Resource {
    pub fn hash self:$Resource -> i64 { self.value std::int_hash }
}

pub struct Holder derives std::Hashable {
    pub value: Resource
}

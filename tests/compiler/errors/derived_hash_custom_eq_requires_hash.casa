# expect: custom derived `eq` method requires an explicit `hash` companion
import "std" as std

pub struct Identifier derives std::Eq std::Hashable {
    pub value: i64
}

impl Identifier: std::PartialEq {
    pub fn eq self:$Identifier other:$Identifier -> bool { other.value 10 % self.value 10 % == }
}

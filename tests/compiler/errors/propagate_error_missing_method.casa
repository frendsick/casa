# expect: UNDEFINED_NAME

enum Partial[T] {
    Stop
    Continue (T)
}

impl Partial {
    fn is_ok [T] self:$Partial[T] -> bool { false }
    fn unwrap [T] self:mut$Partial[T] -> T {
        unsafe { self ptr::from_ref 8 + ptr::read[T] }
    }
}

fn bad -> Partial[str] { 1 Partial::Continue ? }

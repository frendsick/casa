fn exit code:i64 {
    unsafe { code 60 syscall1 drop }
}

unsafe fn memcpy dst:ptr src:ptr n:u64 {
    0 (u64) = i
    while i n > do
        unsafe { src i + load8 }
        unsafe { dst i + store8 }
        1 += i
    done
}

struct RawBuffer {
    data: ptr
}

impl RawBuffer {
    fn new size:u64 -> RawBuffer {
        # SAFETY: RawBuffer owns the complete allocation until destruction.
        unsafe { size alloc RawBuffer }
    }

    fn drop self:mut$RawBuffer {
        # SAFETY: RawBuffer exclusively owns data until drop releases it.
        unsafe {
            self.data free
            0 (ptr) = self.data
        }
    }
}

impl array {
    # The length is part of the type, so it costs nothing at runtime (ADR-0152).
    fn length [T const N:u64] self:$array[T N] -> u64 { N }

    fn is_empty [T const N:u64] self:$array[T N] -> bool { 0 N == }

    fn nth [T const N:u64] self:$array[T N] index:u64 -> $T {
        if N index >= then
            "error: array index out of bounds\n" eprint
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        # An array value is its element storage.
        # SAFETY: The bounds check keeps the computed address inside initialized array storage.
        unsafe { self (ptr) index size_of[T] * + ptr::as_ref[T] }
    }
}

struct List {
    data:     ptr
    size:     u64
    capacity: u64
}

struct Slice[T] {
    source: $List[T]
    start:  u64
    size:   u64
}

impl[T] Slice[T] {
    fn length self:$Slice[T] -> u64 { self.size }

    fn is_empty self:$Slice[T] -> bool { 0 self.size == }

    fn nth self:$Slice[T] index:u64 -> $T {
        if self.size index >= then
            "error: Slice index out of bounds\n" eprint
            1 exit
        fi
        self.start index + self.source.get_ref
    }
}

impl List {
    fn new [T] -> List[T] {
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The allocation creates eight valid element slots with no initialized values.
        unsafe { 8 0 stride 8 * alloc List }
    }

    # The array no longer stores its length, so the list records it (ADR-0152).
    fn from_array[T const N:u64] array:array[T N] -> List[T] {
        N = capacity
        if 0 capacity == then
            1 (u64) = capacity
        fi
        # SAFETY: `array` is owned, and the returned List assumes responsibility for its storage.
        unsafe { capacity N array ptr::into_raw List }
    }

    fn length self:$List -> u64 { self.size }

    fn is_empty self:$List -> bool { 0 self.size == }

    fn get [T] self:$List[T] n:u64 -> $T {
        if self.size n >= 0 n < || then
            "error: List index out of bounds\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The bounds check identifies one initialized element in the allocation.
        unsafe { self.data n stride * + ptr::as_ref[T] }
    }

    fn get_ref [T] self:$List[T] n:u64 -> $T { n self.get }

    fn get_mut [T] self:mut$List[T] n:u64 -> mut$T {
        if self.size n >= 0 n < || then
            "error: List index out of bounds\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The exclusive List borrow and bounds check identify one initialized element.
        unsafe { self.data n stride * + ptr::as_mut[T] }
    }

    fn set [T] self:mut$List[T] n:u64 item:T {
        if self.size n >= 0 n < || then
            "error: List index out of bounds\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The bounds check identifies initialized T storage owned exclusively by the List.
        unsafe {
            self.data n stride * + = address
            address ptr::read[T] drop
            item address ptr::write[T]
        }
    }

    fn replace [T] self:mut$List[T] n:u64 item:T -> T {
        if self.size n >= 0 n < || then
            "error: List index out of bounds\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The bounds check identifies initialized T storage owned exclusively by the List.
        unsafe {
            self.data n stride * + = address
            address ptr::read[T] = previous
            item address ptr::write[T]
            previous
        }
    }

    fn push [T] self:mut$List[T] item:T {
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: Capacity tracks the allocation size, and each initialized element moves once.
        unsafe {
            if self.capacity self.size >= then
                self.capacity 2 * = self.capacity
                self.capacity stride * alloc = new_data
                self.data = old_data
                0 (u64) = index
                while index self.size > do
                    old_data index stride * + ptr::read[T] = existing
                    existing new_data index stride * + ptr::write[T]
                    1 += index
                done
                old_data free
                new_data = self.data
            fi
            item self.data self.size stride * + ptr::write[T]
            1 += self.size
        }
    }

    fn pop [T] self:mut$List[T] -> T {
        if 0 self.size <= then
            "error: pop from empty List\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        1 -= self.size
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The decremented size identifies the last initialized element.
        unsafe { self.data self.size stride * + ptr::read[T] }
    }

    fn insert [T] self:mut$List[T] item:T index:u64 {
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        if 0 index < self.size index > || then
            "error: List insert index out of bounds\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        # SAFETY: Capacity and bounds checks keep all moves inside allocated T storage.
        unsafe {
            # Ensure capacity
            if self.capacity self.size >= then
                self.capacity 2 * = self.capacity
                self.capacity stride * alloc = new_data
                self.data = old_data
                0 (u64) = move_index
                while move_index self.size > do
                    old_data move_index stride * + ptr::read[T] = existing
                    existing new_data move_index stride * + ptr::write[T]
                    1 += move_index
                done
                old_data free
                new_data = self.data
            fi
            # Shift elements right from end to index
            self.size = i
            while i index < do
                self.data i 1 - stride * + ptr::read[T] = shifted
                shifted self.data i stride * + ptr::write[T]
                i 1 - = i
            done
            # Store item at index
            item self.data index stride * + ptr::write[T]
            1 += self.size
        }
    }

    fn remove [T] self:mut$List[T] index:u64 -> Option[T] {
        if self.size index >= then
            Option[T]::None return
        fi
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: The bounds check and size keep every move inside initialized T storage.
        unsafe {
            self.data index stride * + ptr::read[T] = removed
            index = move_index
            while self.size move_index 1 + < do
                self.data move_index 1 + stride * + ptr::read[T] = shifted
                shifted self.data move_index stride * + ptr::write[T]
                1 += move_index
            done
            1 -= self.size
            removed Option::Some
        }
    }

    fn slice [T] self:$List[T] start:u64 stop:u64 -> Slice[T] {
        if self.size stop > stop start > || then
            "error: List slice out of bounds\n" eprint
            1 exit
        fi
        stop start - start self Slice
    }

    fn as_slice [T] self:$List[T] -> Slice[T] { self.size 0 self.slice }

    fn swap_at [T] self:mut$List[T] i:u64 j:u64 {
        if i j == then
            return
        fi
        if self.size i >= self.size j >= || then
            "error: List index out of bounds\n" print
            # SAFETY: The process exits without returning to the caller.
            unsafe { 1 60 syscall1 drop }
        fi
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: Both bounds checks keep each byte swap inside initialized elements.
        unsafe {
            0 (u64) = offset
            while offset stride > do
                self.data i stride * + offset + load8 = first
                self.data j stride * + offset + load8 = second
                second self.data i stride * + offset + store8
                first self.data j stride * + offset + store8
                1 += offset
            done
        }
    }

    fn reverse [T] self:mut$List[T] {
        if 2 self.size < then
            return
        fi
        0 (u64) = left
        self.size 1 - = right
        while left right > do
            left right self.swap_at
            1 += left
            1 -= right
        done
    }

    # Moves every element of `other` onto the end of `self`, consuming `other`.
    fn append [T] self:mut$List[T] other:List[T] {
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        self.size other.size + = combined_size
        # SAFETY: Both Lists own valid allocations, and every initialized element moves once.
        unsafe {
            if self.capacity combined_size > then
                while self.capacity combined_size > do
                    self.capacity 2 * = self.capacity
                done
                self.capacity stride * alloc = new_data
                self.data = old_data
                0 (u64) = index
                while index self.size > do
                    old_data index stride * + ptr::read[T] = existing
                    existing new_data index stride * + ptr::write[T]
                    1 += index
                done
                old_data free
                new_data = self.data
            fi
            0 (u64) = index
            while index other.size > do
                other.data index stride * + ptr::read[T] = existing
                existing self.data self.size index + stride * + ptr::write[T]
                1 += index
            done
            combined_size = self.size
            0 = other.size
            other.data free
            0 (ptr) = other.data
        }
    }

    fn drop [T] self:mut$List[T] {
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        # SAFETY: `size` delimits the initialized elements owned by this List.
        unsafe {
            self.size = index
            while 0 index != do
                1 -= index
                self.data index stride * + ptr::read[T] drop
            done
            self.data free
            0 (ptr) = self.data
            0 = self.size
            0 = self.capacity
        }
    }
}

# ---------------------------------------------------------------------------
# Type conversions
# ---------------------------------------------------------------------------

struct ParsedFloatText {
    digits:   String
    exponent: i64
    negative: bool
}

struct RoundedFloatParts {
    exponent:    i64
    significand: u64
}

fn parse_float_text text:$str -> Option[ParsedFloatText] {
    if 0 text.length == then
        Option::None return
    fi
    0 (u64) = index
    false = negative
    if '-' 0 text.at == then
        true = negative
        1 (u64) = index
        if index text.length == then
            Option::None return
        fi
    fi

    String::new = digits
    0 = fractional_digits
    false = decimal_seen
    false = digit_seen
    while index text.length > do
        index text.at = character
        if character.is_digit then
            true = digit_seen
            character digits.push
            if decimal_seen then
                1 += fractional_digits
            fi
            1 += index
            continue
        fi
        if '.' character == decimal_seen ! && then
            if digit_seen ! then
                Option::None return
            fi
            true = decimal_seen
            1 += index
            if index text.length == then
                Option::None return
            fi
            continue
        fi
        break
    done
    if digit_seen ! then
        Option::None return
    fi

    0 = explicit_exponent
    if index text.length > then
        index text.at = marker
        if 'e' marker != 'E' marker != && then
            Option::None return
        fi
        1 += index
        false = exponent_negative
        if index text.length > then
            index text.at = sign
            if '-' sign == '+' sign == || then
                '-' sign == = exponent_negative
                1 += index
            fi
        fi
        if index text.length == then
            Option::None return
        fi
        while index text.length > do
            index text.at = character
            if character.is_digit ! then
                Option::None return
            fi
            if explicit_exponent 1000 <= then
                Option::None return
            fi
            10 explicit_exponent * character.codepoint i64::from '0'.codepoint i64::from - + = explicit_exponent
            1 += index
        done
        if exponent_negative then
            0 explicit_exponent - = explicit_exponent
        fi
    fi
    explicit_exponent fractional_digits - = exponent
    negative exponent digits ParsedFloatText Option::Some
}

fn rounded_float_parts parsed:$ParsedFloatText precision:u64 -> RoundedFloatParts {
    0 (u64) = significand
    0 (u64) = significant_digits
    -1 = round_digit
    false = trailing_nonzero
    for character in parsed.digits.iter do
        character.codepoint i64::from '0'.codepoint i64::from - = digit
        if 0 significant_digits == 0 digit == && then
            continue
        fi
        if precision significant_digits < then
            10 significand * digit (u64) + = significand
        elif significant_digits precision == then
            digit = round_digit
        elif 0 digit != then
            true = trailing_nonzero
        fi
        1 += significant_digits
    done
    if
        round_digit 5 <
        5 round_digit == trailing_nonzero significand 2 % 0 != || && ||
    then
        1 += significand
    fi
    0 = skipped
    if significant_digits precision < then
        significant_digits (i64) precision (i64) - = skipped
    fi
    significand parsed.exponent skipped + RoundedFloatParts
}

fn format_unsigned_decimal value:u64 -> String {
    20 String::with_capacity = reversed
    value = remaining
    if 0 remaining == then
        '0' reversed.push
    fi
    while 0 remaining != do
        remaining 10 % '0'.codepoint (u64) + (u32)
        char::from_codepoint.unwrap reversed.push
        remaining 10 / = remaining
    done
    reversed.as_str.reverse
}

fn format_float_decimal significand:u64 exponent:i64 negative:bool -> String {
    significand.to_str = digits
    String::new = result
    exponent 1 + = integer_digits
    if 0 exponent >= digits.length (i64) integer_digits <= && then
        integer_digits (u64) = split
        if split digits.length == then
            f"{digits}.0" = result
        else
            digits.length = digits_length
            digits.as_str 0 split str::substring = whole
            digits.as_str split digits_length split - str::substring = fraction
            f"{whole}.{fraction}" = result
        fi
    elif 0 exponent < -4 exponent >= && then
        0 exponent - 1 - (u64) = zero_count
        f"0.{zero_count "0".repeat}{digits}" = result
    else
        if 1 digits.length == then
            f"{digits}e{exponent}" = result
        else
            digits.length = digits_length
            digits.as_str 0 1 str::substring = first
            digits.as_str 1 digits_length 1 - str::substring = rest
            f"{first}.{rest}e{exponent}" = result
        fi
    fi
    if negative then
        f"-{result}"
    else
        result
    fi
}

fn __numeric_try_i8 valid:bool value:i8 -> Option[i8] {
    if valid then
        value Option::Some
    else
        Option[i8]::None
    fi
}

fn __numeric_try_i16 valid:bool value:i16 -> Option[i16] {
    if valid then
        value Option::Some
    else
        Option[i16]::None
    fi
}

fn __numeric_try_i32 valid:bool value:i32 -> Option[i32] {
    if valid then
        value Option::Some
    else
        Option[i32]::None
    fi
}

fn __numeric_try_i64 valid:bool value:i64 -> Option[i64] {
    if valid then
        value Option::Some
    else
        Option[i64]::None
    fi
}

fn __numeric_try_u8 valid:bool value:u8 -> Option[u8] {
    if valid then
        value Option::Some
    else
        Option[u8]::None
    fi
}

fn __numeric_try_u16 valid:bool value:u16 -> Option[u16] {
    if valid then
        value Option::Some
    else
        Option[u16]::None
    fi
}

fn __numeric_try_u32 valid:bool value:u32 -> Option[u32] {
    if valid then
        value Option::Some
    else
        Option[u32]::None
    fi
}

fn __numeric_try_u64 valid:bool value:u64 -> Option[u64] {
    if valid then
        value Option::Some
    else
        Option[u64]::None
    fi
}

fn __numeric_try_f32 valid:bool value:f32 -> Option[f32] {
    if valid then
        value Option::Some
    else
        Option[f32]::None
    fi
}

fn __numeric_try_f64 valid:bool value:f64 -> Option[f64] {
    if valid then
        value Option::Some
    else
        Option[f64]::None
    fi
}

impl i64 {
    fn wrapping_add self:$i64 other:i64 -> i64 { self other + }
    fn wrapping_sub self:$i64 other:i64 -> i64 { self other - }
    fn wrapping_mul self:$i64 other:i64 -> i64 { self other * }

    fn to_str n:$i64 -> String {
        if 0 n < then
            n 0.wrapping_sub = absolute
            if 0 absolute < then
                "-9223372036854775808".to_str
            else
                "-".to_str = result
                absolute (u64) format_unsigned_decimal result.append_string
                result
            fi
        else
            n copy (u64) format_unsigned_decimal
        fi
    }

    fn abs self:$i64 -> i64 {
        if 0 self < then
            0 self -
        else
            self copy
        fi
    }

    fn min self:$i64 other:i64 -> i64 {
        if other self < then
            self copy
        else
            other
        fi
    }

    fn max self:$i64 other:i64 -> i64 {
        if self other < then
            self copy
        else
            other
        fi
    }

    fn clamp self:$i64 lo:i64 hi:i64 -> i64 {
        if lo self < then
            lo
        elif self hi < then
            hi
        else
            self copy
        fi
    }

    fn pow self:$i64 exp:i64 -> i64 {
        if 0 exp < then
            0 return
        fi
        1 = result
        0 = i
        while i exp > do
            self result * = result
            1 += i
        done
        result
    }
}

impl u64 {
    fn to_str n:$u64 -> String { n copy format_unsigned_decimal }

    fn eq self:$u64 other:$u64 -> bool { self other == }
}

impl f32 {
    fn from_bits bits:u32 -> f32 { bits (f32) }
    fn to_bits self:$f32 -> u32 { self copy (u32) }
    fn round_from value:i64 -> f32 { value (f32) }
    fn __float_floor self:$f32 -> f32 { self copy }
    fn __float_ceil self:$f32 -> f32 { self copy }
    fn __float_trunc self:$f32 -> f32 { self copy }
    fn __float_round self:$f32 -> f32 { self copy }

    fn nan -> f32 { 2143289344 = bits:u32 bits f32::from_bits }
    fn infinity -> f32 { 2139095040 = bits:u32 bits f32::from_bits }
    fn neg_infinity -> f32 { 4286578688 = bits:u32 bits f32::from_bits }
    fn epsilon -> f32 { 872415232 = bits:u32 bits f32::from_bits }

    fn is_nan self:$f32 -> bool {
        self.to_bits = bits
        2139095040 bits & 2139095040 ==
        8388607 bits & 0 != &&
    }

    fn is_infinite self:$f32 -> bool { 2147483647 self.to_bits & 2139095040 == }

    fn is_finite self:$f32 -> bool { self.is_infinite ! self.is_nan ! && }
    fn abs self:$f32 -> f32 { 2147483647 self.to_bits & f32::from_bits }
    fn floor self:$f32 -> f32 { self.__float_floor }
    fn ceil self:$f32 -> f32 { self.__float_ceil }
    fn trunc self:$f32 -> f32 { self.__float_trunc }
    fn round self:$f32 -> f32 { self.__float_round }

    fn to_str self:$f32 -> String {
        if self.is_nan then
            "NaN".to_str return
        fi
        if self f32::infinity == then
            "inf".to_str return
        fi
        if self f32::neg_infinity == then
            "-inf".to_str return
        fi
        self.to_bits 2147483648 & 0 != = negative
        if self 0.0 == then
            if negative then "-0.0".to_str else "0.0".to_str fi return
        fi

        self.abs f64::from = normalized
        0 = exponent
        while 10.0 normalized >= do
            normalized 10.0 / = normalized
            1 += exponent
        done
        while 1.0 normalized < do
            10.0 normalized * = normalized
            1 -= exponent
        done
        8 exponent - = scale_exponent
        scale_exponent self.abs f64::from u64::__float_significand = significand
        if 1000000000 significand >= then
            significand 10 / = significand
            1 += exponent
        fi
        negative exponent significand format_float_decimal = result
        if
            result
                .to_f32
                .unwrap
                .to_bits self.to_bits !=
        then
            significand 1 + = adjusted
            negative exponent adjusted format_float_decimal = candidate
            if
                candidate
                    .to_f32
                    .unwrap
                    .to_bits self.to_bits ==
            then
                adjusted = significand
                candidate = result
            elif 0 significand != then
                significand 1 - = adjusted
                negative exponent adjusted format_float_decimal = candidate
                if
                    candidate
                        .to_f32
                        .unwrap
                        .to_bits self.to_bits ==
                then
                    adjusted = significand
                    candidate = result
                fi
            fi
        fi
        while 10 significand >= do
            significand 10 / = shorter
            negative exponent shorter format_float_decimal = candidate
            if candidate.to_f32 Option::Some(parsed) is then
                if parsed.to_bits self.to_bits == then
                    shorter = significand
                    candidate = result
                    continue
                fi
            fi
            shorter 1 + = rounded
            negative exponent rounded format_float_decimal = candidate
            if candidate.to_f32 Option::Some(parsed) is then
                if parsed.to_bits self.to_bits == then
                    rounded = significand
                    candidate = result
                    continue
                fi
            fi
            break
        done
        result
    }
}

impl f64 {
    fn from_bits bits:u64 -> f64 { bits (f64) }
    fn to_bits self:$f64 -> u64 { self copy (u64) }
    fn round_from value:i64 -> f64 { value (f64) }
    fn __float_floor self:$f64 -> f64 { self copy }
    fn __float_ceil self:$f64 -> f64 { self copy }
    fn __float_trunc self:$f64 -> f64 { self copy }
    fn __float_round self:$f64 -> f64 { self copy }

    fn nan -> f64 { 9221120237041090560 = bits:u64 bits f64::from_bits }
    fn infinity -> f64 { 9218868437227405312 = bits:u64 bits f64::from_bits }
    fn neg_infinity -> f64 { 18442240474082181120 = bits:u64 bits f64::from_bits }
    fn epsilon -> f64 { 4372995238176751616 = bits:u64 bits f64::from_bits }

    fn is_nan self:$f64 -> bool {
        self.to_bits = bits
        9218868437227405312 bits & 9218868437227405312 ==
        4503599627370495 bits & 0 != &&
    }

    fn is_infinite self:$f64 -> bool { 9223372036854775807 self.to_bits & 9218868437227405312 == }

    fn is_finite self:$f64 -> bool { self.is_infinite ! self.is_nan ! && }
    fn abs self:$f64 -> f64 { 9223372036854775807 self.to_bits & f64::from_bits }
    fn floor self:$f64 -> f64 { self.__float_floor }
    fn ceil self:$f64 -> f64 { self.__float_ceil }
    fn trunc self:$f64 -> f64 { self.__float_trunc }
    fn round self:$f64 -> f64 { self.__float_round }

    fn to_str self:$f64 -> String {
        if self.is_nan then
            "NaN".to_str return
        fi
        if self f64::infinity == then
            "inf".to_str return
        fi
        if self f64::neg_infinity == then
            "-inf".to_str return
        fi
        self.to_bits 9223372036854775808 & 0 != = negative
        if self 0.0 == then
            if negative then "-0.0".to_str else "0.0".to_str fi return
        fi

        self.abs = normalized
        0 = exponent
        while 10.0 normalized >= do
            normalized 10.0 / = normalized
            1 += exponent
        done
        while 1.0 normalized < do
            10.0 normalized * = normalized
            1 -= exponent
        done
        16 exponent - = scale_exponent
        scale_exponent self.abs u64::__float_significand = significand
        if 100000000000000000 significand >= then
            significand 10 / = significand
            1 += exponent
        fi
        negative exponent significand format_float_decimal = result
        if
            result
                .to_f64
                .unwrap
                .to_bits self.to_bits !=
        then
            significand 1 + = adjusted
            negative exponent adjusted format_float_decimal = candidate
            if
                candidate
                    .to_f64
                    .unwrap
                    .to_bits self.to_bits ==
            then
                adjusted = significand
                candidate = result
            elif 0 significand != then
                significand 1 - = adjusted
                negative exponent adjusted format_float_decimal = candidate
                if
                    candidate
                        .to_f64
                        .unwrap
                        .to_bits self.to_bits ==
                then
                    adjusted = significand
                    candidate = result
                fi
            fi
        fi
        while 10 significand >= do
            significand 10 / = shorter
            negative exponent shorter format_float_decimal = candidate
            if candidate.to_f64 Option::Some(parsed) is then
                if parsed.to_bits self.to_bits == then
                    shorter = significand
                    candidate = result
                    continue
                fi
            fi
            shorter 1 + = rounded
            negative exponent rounded format_float_decimal = candidate
            if candidate.to_f64 Option::Some(parsed) is then
                if parsed.to_bits self.to_bits == then
                    rounded = significand
                    candidate = result
                    continue
                fi
            fi
            break
        done
        result
    }
}

impl bool {
    fn to_str b:$bool -> String {
        if b copy then
            "true".to_str
        else
            "false".to_str
        fi
    }

    fn eq self:$bool other:$bool -> bool { self other == }
}

impl str {
    fn to_f32 self:$str -> Option[f32] {
        if "NaN" self == then
            f32::nan Option::Some return
        fi
        if "inf" self == then
            f32::infinity Option::Some return
        fi
        if "-inf" self == then
            f32::neg_infinity Option::Some return
        fi
        self.to_f64 = wide_opt
        if wide_opt.is_none then
            Option::None return
        fi
        wide_opt.unwrap f32::round_from = value
        if value.is_finite ! then
            Option::None return
        fi
        value Option::Some
    }

    fn to_f64 self:$str -> Option[f64] {
        if "NaN" self == then
            f64::nan Option::Some return
        fi
        if "inf" self == then
            f64::infinity Option::Some return
        fi
        if "-inf" self == then
            f64::neg_infinity Option::Some return
        fi
        self parse_float_text = parsed_opt
        if parsed_opt.is_none then
            Option::None return
        fi
        parsed_opt.unwrap = parsed
        17 parsed rounded_float_parts = parts
        parts.exponent parts.significand f64::__from_decimal_parts = value
        if value.is_finite ! then
            Option::None return
        fi
        if parsed.negative then
            -1.0 value * = value
        fi
        value Option::Some
    }

    fn length s:$str -> u64 {
        unsafe { s (ptr) load64 (u64) }
    }

    fn is_empty self:$str -> bool { 0 self.length == }

    fn at s:$str index:u64 -> char {
        unsafe { s (ptr) 8 + index + load8 (u32) char::from_codepoint_unchecked }
    }

    fn as_cstr s:$str -> Option[cstr] {
        0 (u64) = index
        while index s.length > do
            if 0 index s.at.codepoint == then
                Option[cstr]::None return
            fi
            1 += index
        done
        # SAFETY: str storage has a trailing NUL and the scan rejected interior NUL bytes.
        unsafe { s (ptr) 8 + (cstr) } Option::Some
    }

    fn from_cstr p:ptr -> String {
        0 (u64) = len
        # SAFETY: Callers currently provide an accessible NUL-terminated pointer. #360 will
        # encode this precondition in the borrowed cstr boundary.
        while unsafe { p len + load8 0 != } do
            1 += len
        done
        len String::with_capacity = result
        # SAFETY: The scan found len readable bytes and the result has matching capacity.
        unsafe {
            len result.data (ptr) store64
            len p result.data (ptr) 8 + memcpy
            0 result.data (ptr) 8 + len + store8
        }
        result
    }

    fn eq b:$str a:$str -> bool {
        a.length = length_a
        b.length = length_b
        if length_a length_b != then
            false return
        fi
        true = all_equal
        0 (u64) = compare_index
        while compare_index length_a > do
            if compare_index a.at compare_index b.at != then
                false = all_equal
                break
            fi
            1 += compare_index
        done
        all_equal
    }

    fn substring len:u64 start:u64 s:$str -> String {
        if
            s.length start >
            s.length start - len > ||
            start s.is_char_boundary ! ||
            start len + s.is_char_boundary ! ||
        then
            "error: str substring is not on UTF-8 boundaries\n" eprint
            1 exit
        fi
        len String::with_capacity = result
        # SAFETY: The caller-provided range and result capacity cover len bytes.
        unsafe {
            len result.data (ptr) store64
            len s (ptr) 8 + start + result.data (ptr) 8 + memcpy
            0 result.data (ptr) 8 + len + store8
        }
        result
    }

    fn find needle:$str s:$str -> i64 {
        s.length = source_length
        needle.length = needle_length
        if needle_length source_length < then
            0 1 - return
        fi
        0 1 - = found_pos
        0 (u64) = search_index
        while search_index source_length needle_length - 1 + > do
            true = all_match
            0 (u64) = match_index
            while match_index needle_length > do
                if search_index match_index + s.at match_index needle.at != then
                    false = all_match
                    break
                fi
                1 += match_index
            done
            if all_match then
                search_index (i64) = found_pos
                break
            fi
            1 += search_index
        done
        found_pos
    }

    fn starts_with prefix:$str s:$str -> bool {
        s.length = source_length
        prefix.length = prefix_length
        if prefix_length source_length < then
            false return
        fi
        true = all_match
        0 (u64) = compare_index
        while compare_index prefix_length > do
            if compare_index s.at compare_index prefix.at != then
                false = all_match
                break
            fi
            1 += compare_index
        done
        all_match
    }

    fn ends_with suffix:$str s:$str -> bool {
        s.length = source_length
        suffix.length = suffix_length
        if suffix_length source_length < then
            false return
        fi
        source_length suffix_length - = offset
        true = all_match
        0 (u64) = compare_index
        while compare_index suffix_length > do
            if offset compare_index + s.at compare_index suffix.at != then
                false = all_match
                break
            fi
            1 += compare_index
        done
        all_match
    }

    fn concat b:$str a:$str -> String { f"{a}{b}" }

    fn contains needle:$str s:$str -> bool { s needle str::find 0 1 - != }

    fn is_char_boundary self:$str index:u64 -> bool {
        if 0 index == self.length index == || then
            true return
        fi
        if self.length index >= then
            false return
        fi
        index self.at.codepoint 192 & 128 !=
    }

    fn split delimiter:$str s:$str -> List[String] {
        List[String]::new = parts
        delimiter.length = delim_length
        0 (u64) = scan_pos
        while scan_pos s.length >= do
            # Search for delimiter starting from scan_pos
            s scan_pos s.length scan_pos - str::substring = remaining
            remaining.as_str delimiter str::find = found_index
            if 0 found_index < then
                # No more delimiters, push the rest and stop
                remaining parts.push
                break
            else
                # Push substring before delimiter
                s scan_pos found_index (u64) str::substring parts.push
                found_index (u64) delim_length + scan_pos + = scan_pos
            fi
        done
        parts
    }

    fn trim s:$str -> String {
        s.length = source_length
        0 (u64) = trim_start
        while trim_start source_length > do
            trim_start s.at = character
            if
                ' ' character !=
                '\t' character != &&
                '\n' character != &&
                '\r' character != &&
            then
                break
            fi
            1 += trim_start
        done
        source_length = trim_end
        while trim_start trim_end > do
            trim_end 1 - s.at = character
            if
                ' ' character !=
                '\t' character != &&
                '\n' character != &&
                '\r' character != &&
            then
                break
            fi
            1 -= trim_end
        done
        if trim_start trim_end < then
            String::new
        else
            s trim_start trim_end trim_start - str::substring
        fi
    }

    fn replace old:$str new_str:$str s:$str -> String {
        old.length = old_length
        String::new = builder
        0 (u64) = scan_pos
        while scan_pos s.length >= do
            s scan_pos s.length scan_pos - str::substring = remaining
            remaining.as_str old str::find = found_index
            if 0 found_index < then
                # No more occurrences, append the rest
                remaining.as_str builder.append
                s.length = scan_pos
            else
                # Append part before match
                if 0 found_index != then
                    s scan_pos found_index (u64) str::substring = prefix
                    prefix.as_str builder.append
                fi
                new_str builder.append
                found_index (u64) old_length + scan_pos + = scan_pos
            fi
        done
        builder
    }

    fn to_upper self:$str -> String {
        self.length = len
        len String::with_capacity = result
        # SAFETY: The result allocation has a writable length header.
        unsafe { len result.data (ptr) store64 }
        0 (u64) = idx
        while idx len > do
            idx self.at = ch
            if ch.is_lower then
                ch.codepoint i64::from
                'a'.codepoint i64::from -
                'A'.codepoint i64::from +
                u32::try_from.unwrap char::from_codepoint.unwrap
                = ch
            else
                ch = ch
            fi
            # SAFETY: ASCII case conversion writes one byte within the allocated data range.
            unsafe { ch.codepoint (u8) result.data (ptr) 8 + idx + store8 }
            1 += idx
        done
        # SAFETY: The result allocation has a trailing NUL slot after len bytes.
        unsafe { 0 result.data (ptr) 8 + len + store8 }
        result
    }

    fn to_lower self:$str -> String {
        self.length = len
        len String::with_capacity = result
        # SAFETY: The result allocation has a writable length header.
        unsafe { len result.data (ptr) store64 }
        0 (u64) = idx
        while idx len > do
            idx self.at = ch
            if ch.is_upper then
                ch.codepoint i64::from
                'A'.codepoint i64::from -
                'a'.codepoint i64::from +
                u32::try_from.unwrap char::from_codepoint.unwrap
                = ch
            else
                ch = ch
            fi
            # SAFETY: ASCII case conversion writes one byte within the allocated data range.
            unsafe { ch.codepoint (u8) result.data (ptr) 8 + idx + store8 }
            1 += idx
        done
        # SAFETY: The result allocation has a trailing NUL slot after len bytes.
        unsafe { 0 result.data (ptr) 8 + len + store8 }
        result
    }

    fn repeat self:$str n:u64 -> String {
        if n 0 >= then
            String::new return
        fi
        self.length = len
        len n * = total
        total String::with_capacity = result
        # SAFETY: The result allocation has a writable length header.
        unsafe { total result.data (ptr) store64 }
        0 (u64) = rep
        while rep n > do
            # SAFETY: Each copy writes one non-overlapping len-byte segment within total bytes.
            unsafe { len self (ptr) 8 + result.data (ptr) 8 + rep len * + memcpy }
            1 += rep
        done
        # SAFETY: The result allocation has a trailing NUL slot after total bytes.
        unsafe { 0 result.data (ptr) 8 + total + store8 }
        result
    }

    fn reverse self:$str -> String {
        List[char]::new = characters
        for character in self.iter do
            character characters.push
        done
        self.length String::with_capacity = result
        characters.length = index
        while 0 index != do
            1 -= index
            index characters.get copy result.push
        done
        result
    }

    fn to_int self:$str -> Option[i64] {
        self.length = len
        if 0 len == then
            Option[i64]::None return
        fi
        0 (u64) = idx
        false = negative
        if 0 self.at '-' == then
            if 1 len == then
                Option[i64]::None return
            fi
            true = negative
            1 (u64) = idx
        fi
        0 = result
        while idx len > do
            idx self.at = ch
            if ch.is_digit ! then
                Option[i64]::None return
            fi
            10 result.wrapping_mul = result
            ch.codepoint i64::from '0'.codepoint i64::from - result.wrapping_add = result
            1 += idx
        done
        if negative then
            result 0.wrapping_sub Option::Some
        else
            result Option::Some
        fi
    }
}

impl cstr {
    fn eq self:$cstr other:$cstr -> bool {
        true = result
        true = scanning
        0 (u64) = scan_index
        while scanning do
            unsafe { self (ptr) scan_index + load8 = byte_a }
            unsafe { other (ptr) scan_index + load8 = byte_b }
            if byte_a byte_b != then
                false = result
                false = scanning
            elif 0 byte_a == then
                false = scanning
            else
                1 += scan_index
            fi
        done
        result
    }

    fn to_str s:$cstr -> String {
        # Find length by scanning for null byte
        0 (u64) = source_length
        while unsafe { s (ptr) source_length + load8 0 != } do
            1 += source_length
        done
        source_length String::with_capacity = result
        # SAFETY: The cstr scan established source_length readable bytes before the NUL.
        unsafe {
            source_length result.data (ptr) store64
            source_length s (ptr) result.data (ptr) 8 + memcpy
            0 result.data (ptr) 8 + source_length + store8
        }
        result
    }
}

# ---------------------------------------------------------------------------
# Character classification
# ---------------------------------------------------------------------------

impl char {
    fn from_codepoint codepoint:u32 -> Option[char] {
        if
            1114111 codepoint <=
            55296 codepoint >= 57343 codepoint <= && ! &&
        then
            # SAFETY: The range checks exclude invalid Unicode scalar values.
            unsafe { codepoint char::from_codepoint_unchecked } Option::Some
        else
            Option[char]::None
        fi
    }

    fn is_digit c:char -> bool { '0' c >= '9' c <= && }

    fn is_upper c:char -> bool { 'A' c >= 'Z' c <= && }

    fn is_lower c:char -> bool { 'a' c >= 'z' c <= && }

    fn is_alpha c:char -> bool { c.is_upper c.is_lower || }

    fn is_space c:char -> bool { c ' ' == c '\t' == || c '\n' == || c '\r' == || }

    fn eq self:$char other:$char -> bool { self other == }
    fn lt self:$char other:$char -> bool { other self < }
}

impl ptr {
    fn to_str p:$ptr -> String {
        # SAFETY: Copying and formatting the pointer value does not dereference it.
        unsafe { p copy (i64).to_str }
    }

    fn eq self:$ptr other:$ptr -> bool { self other == }
}

# ---------------------------------------------------------------------------
# Option and Result enums
# ---------------------------------------------------------------------------
# None=0, Some=1 so is_some checks ordinal==1

enum Option[T] {
    None
    Some (T)
}

# Error=0, Ok=1 so is_ok checks ordinal==1

enum Result[T E] {
    Error (E)
    Ok (T)
}

impl Option {
    fn is_ok [T] self:$Option[T] -> bool {
        unsafe { self (ptr) load64 1 == }
    }
    fn is_some [T] self:$Option[T] -> bool {
        unsafe { self (ptr) load64 1 == }
    }
    fn is_none [T] self:$Option[T] -> bool {
        unsafe { self (ptr) load64 0 == }
    }
    fn unwrap [T] self:Option[T] -> T {
        self match
            Option::Some(value) => {
                value return
            }
            Option::None => {
                "error: called unwrap on None\n" print
                60 exit
            }
        end
    }
    fn propagate [T U] self:Option[T] -> Option[U] { Option[U]::None }
    fn unwrap_or [T] self:Option[T] default:T -> T {
        if self.is_some then
            self.unwrap
        else
            default
        fi
    }
    fn map [T U] self:Option[T] f:fn[T -> U] -> Option[U] {
        if self.is_some then
            self.unwrap f exec Option::Some
        else
            Option[U]::None
        fi
    }
    fn and_then [T U] self:Option[T] f:fn[T -> Option[U]] -> Option[U] {
        if self.is_some then
            self.unwrap f exec
        else
            Option[U]::None
        fi
    }
    fn or_else [T] self:Option[T] f:fn[-> Option[T]] -> Option[T] {
        if self.is_some then
            self
        else
            f exec
        fi
    }
    fn filter [T] self:Option[T] f:fn[$T -> bool] -> Option[T] {
        if self.is_none then
            Option[T]::None return
        fi
        self.unwrap = val
        if val f exec then
            val Option::Some
        else
            Option[T]::None
        fi
    }
}

impl Result {
    fn is_ok [T E] self:$Result[T E] -> bool {
        unsafe { self (ptr) load64 1 == }
    }
    fn is_error [T E] self:$Result[T E] -> bool {
        unsafe { self (ptr) load64 0 == }
    }
    fn unwrap [T E] self:Result[T E] -> T {
        self match
            Result::Ok(value) => {
                value return
            }
            Result::Error(_) => {
                "error: called unwrap on error\n" print
                60 exit
            }
        end
    }
    fn unwrap_error [T E] self:Result[T E] -> E {
        self match
            Result::Error(error) => {
                error return
            }
            Result::Ok(_) => {
                "error: called unwrap_error on ok\n" print
                60 exit
            }
        end
    }
    fn propagate [T U E] self:Result[T E] -> Result[U E] { self.unwrap_error Result::Error }
    fn unwrap_or [T E] self:Result[T E] default:T -> T {
        if self.is_ok then
            self.unwrap
        else
            default
        fi
    }
    fn map [T U E] self:Result[T E] f:fn[T -> U] -> Result[U E] {
        if self.is_ok then
            self.unwrap f exec Result::Ok
        else
            self.unwrap_error Result::Error
        fi
    }
    fn map_error [T E F] self:Result[T E] f:fn[E -> F] -> Result[T F] {
        if self.is_ok then
            self.unwrap Result::Ok
        else
            self.unwrap_error f exec Result::Error
        fi
    }
    fn and_then [T U E] self:Result[T E] f:fn[T -> Result[U E]] -> Result[U E] {
        if self.is_ok then
            self.unwrap f exec
        else
            self.unwrap_error Result::Error
        fi
    }
    fn or_else [T E F] self:Result[T E] f:fn[E -> Result[T F]] -> Result[T F] {
        if self.is_ok then
            self.unwrap Result::Ok
        else
            self.unwrap_error f exec
        fi
    }
}

# ---------------------------------------------------------------------------
# Traits
# ---------------------------------------------------------------------------

trait Clone {
    fn clone self:$self -> self
}

trait Copy: Clone { }

impl List {
    # Copy the inline representation of elements that cannot own resources.
    fn shallow_copy [T:Copy] self:$List[T] -> List[T] {
        size_of[T] = stride
        if 0 stride == then
            1 (u64) = stride
        fi
        self.capacity stride * = bytes
        # SAFETY: The copy uses distinct allocations and copies only initialized element bytes.
        unsafe {
            bytes alloc = data
            self.size stride * self.data data memcpy
            self.capacity self.size data List (List[T])
        }
    }
}

trait Eq {
    fn eq self:$self other:$self -> bool
    fn ne self:$self other:$self -> bool { other self.eq ! }
}

trait Ord {
    fn lt self:$self other:$self -> bool
    fn le self:$self other:$self -> bool { self other.lt ! }

    fn gt self:$self other:$self -> bool { self other.lt }

    fn ge self:$self other:$self -> bool { other self.lt ! }
}

trait Word { }

trait Hashable: Eq + Word {
    fn hash self:$self -> i64
}

trait Display: Word {
    fn to_str self:$self -> String
}

trait Iterable[T] {
    fn next self:mut$self -> Option[T]
    fn collect self:mut$self -> List[T] {
        List[T]::new = result
        for elem in self do
            elem result.push
        done
        result
    }

    fn map [U] self:self f:fn[T -> U] -> Iter[U] {
        move {
            self.next = opt
            if opt.is_none then
                Option[U]::None return
            fi
            opt.unwrap f exec Option::Some
        } Iter
    }

    fn filter self:self f:fn[$T -> bool] -> Iter[T] {
        move {
            while true do
                self.next = opt
                if opt.is_none then
                    Option[T]::None return
                fi
                opt.unwrap = elem
                if elem f exec then
                    elem Option::Some return
                fi
            done
            Option[T]::None
        } Iter
    }

    fn fold [U] self:mut$self acc:U f:fn[U T -> U] -> U {
        for elem in self do
            elem acc f exec = acc
        done
        acc
    }

    fn count self:mut$self -> u64 {
        0 (u64) = n
        for elem in self do
            1 += n
        done
        n
    }

    fn any self:mut$self f:fn[$T -> bool] -> bool {
        for elem in self do
            if elem f exec then
                true return
            fi
        done
        false
    }

    fn all self:mut$self f:fn[$T -> bool] -> bool {
        for elem in self do
            if elem f exec ! then
                false return
            fi
        done
        true
    }

    fn find self:mut$self f:fn[$T -> bool] -> Option[T] {
        for elem in self do
            if elem f exec then
                elem Option::Some return
            fi
        done
        Option[T]::None
    }

    fn take self:self n:u64 -> Iter[T] {
        n = remaining
        move {
            if remaining 0 >= then
                Option[T]::None return
            fi
            1 -= remaining
            self.next
        } Iter
    }

    fn skip self:self n:u64 -> Iter[T] {
        false = skipped
        move {
            if skipped ! then
                true = skipped
                0 (u64) = i
                while i n > do
                    self.next = opt
                    if opt.is_none then
                        Option[T]::None return
                    fi
                    1 += i
                done
            fi
            self.next
        } Iter
    }

    fn take_while self:self f:fn[$T -> bool] -> Iter[T] {
        false = finished
        move {
            if finished then
                Option[T]::None return
            fi
            self.next = opt
            if opt.is_none then
                Option[T]::None return
            fi
            opt.unwrap = elem
            if elem f exec then
                elem Option::Some
            else
                true = finished
                Option[T]::None
            fi
        } Iter
    }

    fn skip_while self:self f:fn[$T -> bool] -> Iter[T] {
        false = skipped
        move {
            if skipped ! then
                while true do
                    self.next = opt
                    if opt.is_none then
                        Option[T]::None return
                    fi
                    opt.unwrap = elem
                    if elem f exec ! then
                        true = skipped
                        elem Option::Some return
                    fi
                done
            fi
            self.next
        } Iter
    }

    fn enumerate self:self -> Iter[Pair[i64 T]] {
        0 = index
        move {
            self.next = opt
            if opt.is_none then
                Option[Pair[i64 T]]::None return
            fi
            opt.unwrap index Pair Option::Some
            1 += index
        } Iter
    }

    fn zip [U] self:self other:Iter[U] -> Iter[Pair[T U]] {
        move {
            self.next = opt_a
            if opt_a.is_none then
                Option[Pair[T U]]::None return
            fi
            other.next = opt_b
            if opt_b.is_none then
                Option[Pair[T U]]::None return
            fi
            opt_b.unwrap opt_a.unwrap Pair Option::Some
        } Iter
    }

    fn chain self:self other:Iter[T] -> Iter[T] {
        false = first_finished
        move {
            if first_finished ! then
                self.next = opt
                if opt.is_some then
                    opt return
                fi
                true = first_finished
            fi
            other.next
        } Iter
    }

    fn flat_map [U] self:self f:fn[T -> Iter[U]] -> Iter[U] {
        { Option[U]::None } Iter = inner
        false = has_inner
        move {
            while true do
                if has_inner then
                    inner.next = opt
                    if opt.is_some then
                        opt return
                    fi
                    false = has_inner
                fi
                self.next = outer_opt
                if outer_opt.is_none then
                    Option[U]::None return
                fi
                outer_opt.unwrap f exec = inner
                true = has_inner
            done
            Option[U]::None
        } Iter
    }

    fn reduce self:mut$self f:fn[T T -> T] -> Option[T] {
        self.next = first
        if first.is_none then
            first return
        fi
        first.unwrap = acc
        for elem in self do
            elem acc f exec = acc
        done
        acc Option::Some
    }

    fn min_by self:mut$self f:fn[$T $T -> bool] -> Option[T] {
        self.next = first
        if first.is_none then
            first return
        fi
        first.unwrap = best
        for elem in self do
            if best elem f exec then
                elem = best
            fi
        done
        best Option::Some
    }

    fn max_by self:mut$self f:fn[$T $T -> bool] -> Option[T] {
        self.next = first
        if first.is_none then
            first return
        fi
        first.unwrap = best
        for elem in self do
            if elem best f exec then
                elem = best
            fi
        done
        best Option::Some
    }

    fn partition self:mut$self f:fn[$T -> bool] -> Pair[List[T] List[T]] {
        List[T]::new = yes
        List[T]::new = no
        for elem in self do
            if elem f exec then
                elem yes.push
            else
                elem no.push
            fi
        done
        no yes Pair
    }

    fn sum self:mut$self -> i64 {
        0 = total
        for elem in self do
            elem (i64) += total
        done
        total
    }
}

impl i8: Clone + Copy {
    fn clone self:$i8 -> i8 { self copy }
}

impl i16: Clone + Copy {
    fn clone self:$i16 -> i16 { self copy }
}

impl i32: Clone + Copy {
    fn clone self:$i32 -> i32 { self copy }
}

impl i64: Clone + Copy {
    fn clone self:$i64 -> i64 { self copy }
}

impl u8: Clone + Copy {
    fn clone self:$u8 -> u8 { self copy }
}

impl u16: Clone + Copy {
    fn clone self:$u16 -> u16 { self copy }
}

impl u32: Clone + Copy {
    fn clone self:$u32 -> u32 { self copy }
}

impl u64: Clone + Copy {
    fn clone self:$u64 -> u64 { self copy }
}

impl f32: Clone + Copy {
    fn clone self:$f32 -> f32 { self copy }
}

impl f64: Clone + Copy {
    fn clone self:$f64 -> f64 { self copy }
}

impl bool: Clone + Copy {
    fn clone self:$bool -> bool { self copy }
}

impl char: Clone + Copy {
    fn clone self:$char -> char { self copy }
}

impl cstr: Clone + Copy {
    fn clone self:$cstr -> cstr { self copy }
}

impl ptr: Clone + Copy {
    fn clone self:$ptr -> ptr { self copy }
}

impl str: Clone + Copy {
    fn clone self:$str -> str {
        # SAFETY: Cloning a str copies its non-owning view representation.
        unsafe { self (ptr) (str) }
    }
}

impl str {
    fn as_str self:$str -> $str { self }

    fn to_str self:$str -> String { self String::from_str }
}

impl String: Clone {
    fn clone self:$String -> String { self.as_str String::from_str }
}

impl String {
    fn to_str self:$String -> String { self.clone }
}

impl[T: Clone const N: u64] array[T N]: Clone {
    fn clone self:$array[T N] -> array[T N] {
        # A zero-length array is inhabited, so it still owns storage (ADR-0155).
        N size_of[T] * = bytes
        if 0 bytes == then
            1 (u64) = bytes
        fi
        # SAFETY: The allocation has N element slots, and each receives one valid clone.
        unsafe {
            bytes alloc = data
            0 (u64) = index
            while index N > do
                index self.nth = element
                element T::clone data index size_of[T] * + ptr::write[T]
                1 += index
            done
            data (array[T N])
        }
    }
}

impl[T: Clone] List[T]: Clone {
    fn clone self:$List[T] -> List[T] {
        List[T]::new = result
        0 (u64) = index
        while index self.length > do
            index self.get T::clone result.push
            1 += index
        done
        result
    }
}

impl[T: Clone] Option[T]: Clone {
    fn clone self:$Option[T] -> Option[T] {
        if self Option::Some(value) is then
            value T::clone Option::Some
        else
            Option[T]::None
        fi
    }
}

impl[T: Clone, E: Clone] Result[T E]: Clone {
    fn clone self:$Result[T E] -> Result[T E] {
        if self Result::Ok(value) is then
            value T::clone Result::Ok return
        fi
        if self Result::Error(error) is then
            error E::clone Result::Error return
        fi
        1 exit
    }
}

impl i8: Word { }

impl i16: Word { }

impl i32: Word { }

impl u8: Word { }

impl u16: Word { }

impl u32: Word { }

impl u64: Eq + Display { }

impl f32: Display { }

impl f64: Display { }

impl bool: Eq + Display { }

impl cstr: Eq + Display { }

impl ptr: Eq + Display { }

impl ptr: Hashable {
    fn hash self:$ptr -> i64 {
        # SAFETY: Hashing observes the opaque address bits without dereferencing them.
        unsafe { self copy (i64) } int_hash
    }
}

# ---------------------------------------------------------------------------
# Pair - generic two-element tuple
# ---------------------------------------------------------------------------

struct Pair[A B] {
    first:  A
    second: B
}

impl[A: Display, B: Display] Pair[A B]: Display {
    fn to_str self:$Pair[A B] -> String {
        self.first A::to_str = first_text
        self.second B::to_str = second_text
        f"({first_text}, {second_text})"
    }
}

# ---------------------------------------------------------------------------
# Iter - closure-based lazy iterator
# ---------------------------------------------------------------------------
# A `for` loop calls `<value>::next` repeatedly until it returns `None`. The
# `Iter` struct wraps a closure that yields elements lazily, enabling
# composition of iterator combinators without intermediate allocations.

struct Iter[T] {
    next_fn: fn[-> Option[T]]
}

impl[T] Iter[T]: Iterable[T] {
    fn next self:mut$Iter[T] -> Option[T] { self.next_fn exec }
}

impl[T: Ord] Iter[T] {
    fn min self:mut$Iter[T] -> Option[T] {
        self.next = first
        if first.is_none then
            first return
        fi
        first.unwrap = best
        for elem in self do
            if best elem < then
                elem = best
            fi
        done
        best Option::Some
    }

    fn max self:mut$Iter[T] -> Option[T] {
        self.next = first
        if first.is_none then
            first return
        fi
        first.unwrap = best
        for elem in self do
            if elem best < then
                elem = best
            fi
        done
        best Option::Some
    }
}

impl array {
    # Yields element borrows: the array keeps owning every element, so an
    # owned yield would destroy values the array still references.
    fn iter [T const N:u64] self:$array[T N] -> Iter[$T] {
        0 (u64) = index
        {
            if N index >= then
                Option::None return
            fi
            index self.nth Option::Some
            1 += index
        } Iter
    }
}

impl[T] List[T] {
    # Yields element borrows: the list keeps owning every element, so an owned
    # yield would destroy values the list still references.
    fn iter self:$List[T] -> Iter[$T] {
        0 (u64) = index
        {
            if self.length index >= then
                Option::None return
            fi
            index self.get_ref Option::Some
            1 += index
        } Iter
    }

    fn sort_by self:mut$List[T] f:fn[$T $T -> bool] {
        if 1 self.size <= then
            return
        fi
        f self.size 1 - 0 self.sort_by_range
    }

    fn sort_by_range self:mut$List[T] low:u64 high:u64 f:$fn[$T $T -> bool] {
        if high low >= then
            return
        fi
        high low + 2 / high self.swap_at
        low = store_idx
        low = i
        while i high > do
            if high self.get i self.get f exec then
                store_idx i self.swap_at
                1 += store_idx
            fi
            1 += i
        done
        store_idx high self.swap_at
        if low store_idx > then
            f store_idx 1 - low self.sort_by_range
        fi
        if store_idx high > then
            f high store_idx 1 + self.sort_by_range
        fi
    }
}

impl[T] Slice[T] {
    fn iter self:$Slice[T] -> Iter[$T] {
        0 (u64) = index
        {
            if self.length index >= then
                Option::None return
            fi
            index self.nth Option::Some
            1 += index
        } Iter
    }
}

impl[T: Ord] List[T] {
    fn sort self:mut$List[T] {
        { < } self.sort_by
    }
}

impl List[str] {
    fn join self:$List[str] separator:$str -> String {
        String::new = builder
        0 (u64) = idx
        while idx self.length > do
            if 0 idx > then
                separator builder.append
            fi
            idx self.get builder.append
            1 += idx
        done
        builder
    }

    fn contains self:$List[str] needle:$str -> bool {
        0 (u64) = i
        while i self.length > do
            if needle i self.get == then
                true return
            fi
            1 += i
        done
        false
    }
}

impl List[String] {
    fn join_strings self:$List[String] separator:$str -> String {
        String::new = result
        0 (u64) = index
        while index self.length > do
            if 0 index > then
                separator result.append
            fi
            index self.get.as_str result.append
            1 += index
        done
        result
    }
}

impl array {
    fn contains [const N:u64] self:$array[str N] needle:$str -> bool {
        0 (u64) = i
        while i N > do
            i self.nth = element
            if needle element == then
                true return
            fi
            1 += i
        done
        false
    }
}

impl str {
    fn iter self:$str -> Iter[char] {
        0 (u64) = index
        {
            if self.length index >= then
                Option[char]::None return
            fi
            index self.at.codepoint = first
            if 127 first <= then
                1 += index
                # SAFETY: Valid str storage makes the decoded ASCII byte a Unicode scalar.
                unsafe { first char::from_codepoint_unchecked } Option::Some return
            fi
            if 224 first & 192 == then
                index 1 + self.at.codepoint = second
                2 += index
                # SAFETY: Valid str storage makes the decoded two-byte sequence a Unicode scalar.
                unsafe { first 31 & 6 << second 63 & | char::from_codepoint_unchecked }
                Option::Some return
            fi
            if 240 first & 224 == then
                index 1 + self.at.codepoint = second
                index 2 + self.at.codepoint = third
                3 += index
                # SAFETY: Valid str storage makes the decoded three-byte sequence a Unicode scalar.
                unsafe {
                    first 15 & 12 <<
                    second 63 & 6 << |
                    third 63 & |
                    char::from_codepoint_unchecked
                } Option::Some return
            fi
            index 1 + self.at.codepoint = second
            index 2 + self.at.codepoint = third
            index 3 + self.at.codepoint = fourth
            4 += index
            # SAFETY: Valid str storage makes the decoded four-byte sequence a Unicode scalar.
            unsafe {
                first 7 & 18 <<
                second 63 & 12 << |
                third 63 & 6 << |
                fourth 63 & |
                char::from_codepoint_unchecked
            } Option::Some
        } Iter
    }
}

# ---------------------------------------------------------------------------
# Hash helpers
# ---------------------------------------------------------------------------

fn str_hash s:$str -> i64 {
    5381 = hash
    for sh_char in s.iter do
        hash 5 << = shifted
        shifted hash.wrapping_add = hash
        sh_char.codepoint i64::from hash.wrapping_add = hash
    done
    if 0 hash < then
        hash 0.wrapping_sub = hash
        if 0 hash < then
            0 = hash
        fi
    fi
    hash
}

fn int_hash n:i64 -> i64 {
    if 0 n < then
        n 0.wrapping_sub = absolute
        if 0 absolute < then
            0
        else
            absolute
        fi
    else
        n
    fi
}

impl str: Hashable + Display {
    fn hash self:$str -> i64 { self str_hash }
}

impl String: Hashable + Display {
    fn hash self:$String -> i64 { self.as_str str_hash }

    fn eq self:$String other:$String -> bool { self.as_str other.as_str == }
}

impl i64: Hashable + Ord + Display {
    fn hash self:$i64 -> i64 { self copy int_hash }
    fn eq self:$i64 other:$i64 -> bool { self other == }
    fn lt self:$i64 other:$i64 -> bool { other self < }
}

# ---------------------------------------------------------------------------
# Map[K V] - Hash map with separate chaining
# ---------------------------------------------------------------------------
# Entry layout is the key bytes, value bytes, then the next pointer.
# Bucket array: capacity * 8 bytes (each slot is a ptr to entry or 0)
const MAP_INITIAL_CAP 16

struct Map {
    buckets:  ptr
    size:     u64
    capacity: u64
}

impl[K V] Map[K V] {
    fn entry_key self:$Map[K V] address:ptr -> $K {
        # SAFETY: Callers pass an initialized entry address whose first field is K.
        unsafe { address ptr::as_ref[K] }
    }

    fn entry_value self:$Map[K V] address:ptr -> $V {
        # SAFETY: Callers add the key size to an initialized entry address.
        unsafe { address ptr::as_ref[V] }
    }

    fn drop self:mut$Map[K V] {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        # SAFETY: Every bucket link identifies one initialized node owned by this Map.
        unsafe {
            0 (u64) = bucket_index
            while bucket_index self.capacity > do
                self.buckets bucket_index 8 * + load64 = current_entry
                while 0 current_entry != do
                    current_entry (ptr) next_offset + load64 = next_entry
                    current_entry (ptr) ptr::read[K] drop
                    current_entry (ptr) key_size + ptr::read[V] drop
                    current_entry (ptr) free
                    next_entry = current_entry
                done
                1 += bucket_index
            done
            self.buckets free
            0 (ptr) = self.buckets
            0 = self.size
            0 = self.capacity
        }
    }
}

impl[K: Hashable, V] Map[K V] {
    fn new -> Map[K V] {
        # SAFETY: The allocation contains MAP_INITIAL_CAP pointer-sized bucket slots.
        unsafe {
            MAP_INITIAL_CAP (u64) 8 * alloc = mb
            0 (u64) = bucket_index
            while bucket_index MAP_INITIAL_CAP > do
                0 mb bucket_index 8 * + store64
                1 += bucket_index
            done
            MAP_INITIAL_CAP 0 mb Map (Map[K V])
        }
    }

    fn length self:$Map[K V] -> u64 { self.size }

    fn is_empty self:$Map[K V] -> bool { 0 self.size == }

    fn get self:$Map[K V] key:$K -> Option[$V] {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key K::hash(u64) self.capacity % = bucket_index
        # SAFETY: Bucket links point to initialized key, value, and next fields.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[K] = entry_key
                if key entry_key K::eq then
                    current_entry (ptr) key_size + ptr::as_ref[V] Option::Some return
                fi
                current_entry (ptr) next_offset + load64 = current_entry
            done
            Option[$V]::None
        }
    }

    fn get_mut self:mut$Map[K V] key:$K -> Option[mut$V] {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key K::hash(u64) self.capacity % = bucket_index
        # SAFETY: The exclusive Map borrow and bucket links identify one initialized value.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[K] = entry_key
                if key entry_key K::eq then
                    current_entry (ptr) key_size + ptr::as_mut[V] Option::Some return
                fi
                current_entry (ptr) next_offset + load64 = current_entry
            done
            Option[mut$V]::None
        }
    }

    fn has self:$Map[K V] key:$K -> bool { key self.get.is_some }

    fn set self:mut$Map[K V] key:K value:V {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key K::hash(u64) self.capacity % = bucket_index
        # SAFETY: Entry sizes define disjoint key, value, and link fields in every node.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[K] = entry_key
                if key entry_key K::eq then
                    key drop
                    current_entry (ptr) key_size + ptr::read[V] drop
                    value current_entry (ptr) key_size + ptr::write[V]
                    return
                fi
                current_entry (ptr) next_offset + load64 = current_entry
            done
            # New entry
            next_offset 8 + alloc = new_node
            key new_node (ptr) ptr::write[K]
            value new_node (ptr) key_size + ptr::write[V]
            self.buckets bucket_index 8 * + load64 new_node (ptr) next_offset + store64
            new_node (u64) self.buckets bucket_index 8 * + store64
            1 += self.size
            # Resize at 75% load
            if self.capacity 3 * 4 / self.size >= then
                self.resize
            fi
        }
    }

    fn remove self:mut$Map[K V] key:$K -> Option[V] {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key K::hash(u64) self.capacity % = bucket_index
        0 (u64) = prev_entry
        # SAFETY: Bucket links delimit initialized entries owned exclusively by the Map.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[K] = entry_key
                if key entry_key K::eq ! then
                    current_entry = prev_entry
                    current_entry (ptr) next_offset + load64 = current_entry
                    continue
                fi
                current_entry (ptr) next_offset + load64 = next_entry
                if 0 prev_entry == then
                    next_entry self.buckets bucket_index 8 * + store64
                else
                    next_entry prev_entry (ptr) next_offset + store64
                fi
                1 -= self.size
                current_entry (ptr) ptr::read[K] drop
                current_entry (ptr) key_size + ptr::read[V] = removed
                current_entry (ptr) free
                removed Option::Some return
            done
            Option[V]::None
        }
    }

    fn resize self:mut$Map[K V] {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        self.capacity 2 * = new_capacity
        # SAFETY: Both bucket arrays have pointer-sized slots, and each entry link is rewritten once.
        unsafe {
            new_capacity 8 * alloc = new_buckets
            0 (u64) = rehash_index
            while rehash_index new_capacity > do
                0 new_buckets rehash_index 8 * + store64
                1 += rehash_index
            done
            0 (u64) = rehash_index
            while rehash_index self.capacity > do
                self.buckets rehash_index 8 * + load64 = current_entry
                while 0 current_entry != do
                    current_entry (ptr) next_offset + load64 = next_entry
                    current_entry (ptr) ptr::as_ref[K] K::hash(u64) new_capacity % = bucket_index
                    new_buckets bucket_index 8 * + load64 current_entry (ptr) next_offset + store64
                    current_entry new_buckets bucket_index 8 * + store64
                    next_entry = current_entry
                done
                1 += rehash_index
            done
            self.buckets = old_buckets
            new_buckets = self.buckets
            new_capacity = self.capacity
            old_buckets free
        }
    }

    fn iter self:$Map[K V] -> Iter[Pair[$K $V]] {
        size_of[K] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        0 (u64) = bucket_index
        0 (u64) = current_entry
        {
            # SAFETY: The state and bucket links stay valid for the shared Map borrow.
            unsafe {
                while 0 current_entry == do
                    if self.capacity bucket_index >= then
                        Option::None return
                    fi
                    self.buckets bucket_index 8 * + load64 = current_entry
                    1 += bucket_index
                done
                current_entry (ptr) next_offset + load64 = next_entry
                current_entry (ptr) key_size + self.entry_value
                current_entry (ptr) self.entry_key
                next_entry = current_entry
                Pair Option::Some
            }
        } Iter
    }

    fn iter_keys self:$Map[K V] -> Iter[$K] {
        size_of[K] size_of[V] + = next_offset
        0 (u64) = bucket_index
        0 (u64) = current_entry
        {
            # SAFETY: The state and bucket links stay valid for the shared Map borrow.
            unsafe {
                while 0 current_entry == do
                    if self.capacity bucket_index >= then
                        Option::None return
                    fi
                    self.buckets bucket_index 8 * + load64 = current_entry
                    1 += bucket_index
                done
                current_entry (ptr) next_offset + load64 = next_entry
                current_entry (ptr) self.entry_key Option::Some
                next_entry = current_entry
            }
        } Iter
    }
}

impl[V] Map[String V] {
    fn set_str self:mut$Map[String V] key:$str value:V { value key.to_str self.set }

    # Lookup by a borrowed view avoids allocating a temporary String key.
    fn get_str self:$Map[String V] key:$str -> Option[$V] {
        size_of[String] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key str_hash (u64) self.capacity % = bucket_index
        # SAFETY: Bucket links delimit initialized String and V entries owned by the Map.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[String] = entry_key
                if key entry_key.as_str == then
                    current_entry (ptr) key_size + ptr::as_ref[V] Option::Some return
                fi
                current_entry (ptr) next_offset + load64 = current_entry
            done
            Option[$V]::None
        }
    }

    fn get_mut_str self:mut$Map[String V] key:$str -> Option[mut$V] {
        size_of[String] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key str_hash (u64) self.capacity % = bucket_index
        # SAFETY: The exclusive Map borrow permits an exclusive borrow of the matched value.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[String] = entry_key
                if key entry_key.as_str == then
                    current_entry (ptr) key_size + ptr::as_mut[V] Option::Some return
                fi
                current_entry (ptr) next_offset + load64 = current_entry
            done
            Option[mut$V]::None
        }
    }

    fn has_str self:$Map[String V] key:$str -> bool { key self.get_str.is_some }

    fn remove_str self:mut$Map[String V] key:$str -> Option[V] {
        size_of[String] = key_size
        size_of[V] = value_size
        key_size value_size + = next_offset
        key str_hash (u64) self.capacity % = bucket_index
        0 (u64) = prev_entry
        # SAFETY: Bucket links delimit initialized entries, and removal transfers V ownership.
        unsafe {
            self.buckets bucket_index 8 * + load64 = current_entry
            while 0 current_entry != do
                current_entry (ptr) ptr::as_ref[String] = entry_key
                if key entry_key.as_str != then
                    current_entry = prev_entry
                    current_entry (ptr) next_offset + load64 = current_entry
                    continue
                fi
                current_entry (ptr) next_offset + load64 = next_entry
                if 0 prev_entry == then
                    next_entry self.buckets bucket_index 8 * + store64
                else
                    next_entry prev_entry (ptr) next_offset + store64
                fi
                1 -= self.size
                current_entry (ptr) ptr::read[String] drop
                current_entry (ptr) key_size + ptr::read[V] = removed
                current_entry (ptr) free
                removed Option::Some return
            done
            Option[V]::None
        }
    }
}

impl[K: Hashable + Clone, V] Map[K V] {
    fn keys self:$Map[K V] -> List[K] {
        List[K]::new = key_list
        for pair in self.iter do
            pair.first K::clone key_list.push
        done
        key_list
    }
}

impl[K: Hashable, V: Copy] Map[K V] {
    fn get_copy self:$Map[K V] key:$K -> Option[V] {
        if key self.get Option::Some(value) is then
            value copy Option::Some
        else
            Option[V]::None
        fi
    }
}

impl[V: Copy] Map[String V] {
    fn get_copy_str self:$Map[String V] key:$str -> Option[V] {
        if key self.get_str Option::Some(value) is then
            value copy Option::Some
        else
            Option[V]::None
        fi
    }
}

impl[K: Hashable, V: Clone] Map[K V] {
    fn get_cloned self:$Map[K V] key:$K -> Option[V] {
        if key self.get Option::Some(value) is then
            value V::clone Option::Some
        else
            Option[V]::None
        fi
    }

    fn values self:$Map[K V] -> List[V] {
        List[V]::new = value_list
        for pair in self.iter do
            pair.second V::clone value_list.push
        done
        value_list
    }
}

impl[V: Clone] Map[String V] {
    fn get_cloned_str self:$Map[String V] key:$str -> Option[V] {
        if key self.get_str Option::Some(value) is then
            value V::clone Option::Some
        else
            Option[V]::None
        fi
    }
}

impl[K: Hashable + Clone, V: Clone] Map[K V]: Clone {
    fn clone self:$Map[K V] -> Map[K V] {
        Map[K V]::new = result
        for pair in self.iter do
            pair.second V::clone pair.first K::clone result.set
        done
        result
    }
}

# ---------------------------------------------------------------------------
# Set[K] - Hash set backed by Map
# ---------------------------------------------------------------------------

struct Set[K] {
    map: Map[K i64]
}

impl[K: Hashable] Set[K] {
    fn new -> Set[K] { Map[K i64]::new Set }

    fn length self:$Set[K] -> u64 { self.map.length }

    fn is_empty self:$Set[K] -> bool { self.map.is_empty }

    fn has self:$Set[K] key:$K -> bool { key self.map.has }

    fn add self:mut$Set[K] key:K { 0 key self.map.set }

    fn remove self:mut$Set[K] key:$K { key self.map.remove drop }

    fn iter self:$Set[K] -> Iter[$K] { self.map.iter_keys }
}

impl Set[String] {
    fn add_str self:mut$Set[String] key:$str { key.to_str self.add }

    fn has_str self:$Set[String] key:$str -> bool { key self.map.has_str }

    fn remove_str self:mut$Set[String] key:$str { key self.map.remove_str drop }
}

impl List[String] {
    fn push_str self:mut$List[String] value:$str { value.to_str self.push }
}

impl[K: Hashable + Clone] Set[K] {
    fn to_list self:$Set[K] -> List[K] { self.map.keys }
}

impl[K: Hashable + Clone] Set[K]: Clone {
    fn clone self:$Set[K] -> Set[K] {
        Set[K]::new = result
        for key in self.iter do
            key K::clone result.add
        done
        result
    }
}

# ---------------------------------------------------------------------------
# Command-line arguments
# ---------------------------------------------------------------------------

fn get_arg n:u64 -> String {
    if argc n >= then
        "error: argument index out of bounds\n" eprint
        unsafe { 1 60 syscall1 drop }
    fi
    # SAFETY: argv contains argc accessible NUL-terminated process argument pointers.
    unsafe { argv n 8 * + load64 (cstr) cstr::to_str }
}

# ---------------------------------------------------------------------------
# Stdout output
# ---------------------------------------------------------------------------

fn println message:$str { message print "\n" print }

fn println_string message:String { message print "\n" print }

# ---------------------------------------------------------------------------
# Stderr output
# ---------------------------------------------------------------------------

fn eprint message:$str {
    # SAFETY: The borrowed text contains `length` readable bytes after its header.
    unsafe { message.length message (ptr) 8 + 2 1 syscall3 drop }
}

fn eprintln message:$str { f"{message}\n".as_str eprint }

fn eprintln_string message:String { message.as_str eprint "\n" eprint }

# ---------------------------------------------------------------------------
# String
# ---------------------------------------------------------------------------

fn convert_chars_to_string chars:$List[char] -> String {
    String::new = result
    for character in chars.iter do
        character copy result.push
    done
    result
}

struct String {
    data: str
}

impl String {
    fn with_capacity capacity:u64 -> String {
        # SAFETY: The allocation stores a str header, capacity bytes, and a trailing NUL.
        unsafe {
            capacity 9 + alloc (str) = data
            0 data (ptr) store64
            0 data (ptr) 8 + store8
            data String
        }
    }

    fn new -> String { 0 String::with_capacity }

    fn from_str text:$str -> String {
        text.length String::with_capacity = result
        text result.append
        result
    }

    fn as_str self:$String -> $str {
        # SAFETY: String owns a live str allocation for the duration of the returned borrow.
        unsafe { self (ptr) ptr::as_ref[str] }
    }

    fn length self:$String -> u64 { self.data.length }

    fn capacity self:$String -> u64 {
        # SAFETY: String.data is the complete live allocation returned by alloc.
        unsafe { self.data (ptr) 8 - load64 (u64) 9 - }
    }

    fn is_empty self:$String -> bool { 0 self.length == }

    fn at self:$String index:u64 -> char { index self.as_str.at }

    fn to_int self:$String -> Option[i64] { self.as_str.to_int }

    fn to_f32 self:$String -> Option[f32] { self.as_str.to_f32 }

    fn to_f64 self:$String -> Option[f64] { self.as_str.to_f64 }

    fn iter self:$String -> Iter[char] { self.as_str.iter }

    fn reserve self:mut$String additional:u64 {
        self.length additional + = required
        if self.capacity required <= then
            return
        fi
        self.capacity 2 * = new_capacity
        if required new_capacity < then
            required = new_capacity
        fi
        self.length = length
        # SAFETY: The new buffer has capacity for the existing bytes and trailing NUL.
        unsafe {
            new_capacity 9 + alloc (str) = replacement_data
            length replacement_data (ptr) store64
            length 1 + self.data (ptr) 8 + replacement_data (ptr) 8 + memcpy
            self.data (ptr) = old_data
            replacement_data = self.data
            old_data free
        }
    }

    fn append self:mut$String text:$str {
        self.length = old_length
        text.length = added_length
        added_length self.reserve
        # SAFETY: reserve provides space for the appended bytes and trailing NUL.
        unsafe {
            added_length text (ptr) 8 + self.data (ptr) 8 + old_length + memcpy
            old_length added_length + self.data (ptr) store64
            0 self.data (ptr) 8 + old_length + added_length + store8
        }
    }

    fn append_string self:mut$String text:String { text.as_str self.append }

    fn push self:mut$String character:char {
        character.codepoint = codepoint
        4 (u64) = byte_length
        if 127 codepoint <= then
            1 (u64) = byte_length
        elif 2047 codepoint <= then
            2 (u64) = byte_length
        elif 65535 codepoint <= then
            3 (u64) = byte_length
        fi
        self.length = old_length
        byte_length self.reserve
        # SAFETY: reserve provides four bytes for the encoded scalar and the trailing NUL.
        unsafe {
            self.data (ptr) 8 + old_length + = destination
            if 1 byte_length == then
                codepoint (u8) destination store8
            elif 2 byte_length == then
                codepoint 6 >> 192 | (u8) destination store8
                codepoint 63 & 128 | (u8) destination 1 + store8
            elif 3 byte_length == then
                codepoint 12 >> 224 | (u8) destination store8
                codepoint 6 >> 63 & 128 | (u8) destination 1 + store8
                codepoint 63 & 128 | (u8) destination 2 + store8
            else
                codepoint 18 >> 240 | (u8) destination store8
                codepoint 12 >> 63 & 128 | (u8) destination 1 + store8
                codepoint 6 >> 63 & 128 | (u8) destination 2 + store8
                codepoint 63 & 128 | (u8) destination 3 + store8
            fi
            old_length byte_length + self.data (ptr) store64
            0 destination byte_length + store8
        }
    }

    # SAFETY: The caller must append bytes that preserve the String's UTF-8 invariant.
    unsafe fn push_byte_unchecked self:mut$String byte:u8 {
        self.length = old_length
        1 self.reserve
        # SAFETY: reserve provides one writable byte and the trailing NUL slot.
        unsafe {
            byte self.data (ptr) 8 + old_length + store8
            old_length 1 + self.data (ptr) store64
            0 self.data (ptr) 8 + old_length 1 + + store8
        }
    }

    fn clear self:mut$String {
        # SAFETY: Every String allocation contains its length header and trailing NUL slot.
        unsafe {
            0 self.data (ptr) store64
            0 self.data (ptr) 8 + store8
        }
    }

    fn concat self:String suffix:String -> String {
        suffix.as_str self.append
        self
    }

    fn drop self:mut$String {
        # SAFETY: String exclusively owns data until drop releases the allocation.
        unsafe {
            self.data (ptr) free
            0 (ptr) (str) = self.data
        }
    }
}

# ---------------------------------------------------------------------------
# Process execution
# ---------------------------------------------------------------------------

fn run_command args:List[String] -> i64 {
    for argument in args.iter do
        if
            argument
                .as_str
                .as_cstr
                .is_none
        then
            0 22 - return
        fi
    done
    # fork()
    unsafe { 57 syscall0 = child_pid }
    if child_pid 0 > then
        "error: fork failed\n" eprint
        unsafe { 1 60 syscall1 drop }
    fi
    if 0 child_pid == then
        # Child process: build execve argv array
        args.length 1 + 8 * RawBuffer::new = argv_buf
        0 (u64) = argv_index
        while argv_index args.length > do
            # SAFETY: argv_buf has one pointer slot per argument and every argument passed
            # the interior-NUL check above.
            unsafe {
                argv_index args
                    .get
                    .as_str
                    .as_cstr
                    .unwrap (ptr) (u64)
                argv_buf.data argv_index 8 * + store64
            }
            1 += argv_index
        done
        # Null terminator
        unsafe { 0 argv_buf.data args.length 8 * + store64 }
        unsafe { envp argv_buf.data argv_buf.data load64 (ptr) 59 syscall3 drop }
        # If execve returns, it failed — exit directly via syscall
        argv_buf drop
        unsafe { 1 60 syscall1 drop }
    fi
    # Parent: wait4(pid, &status, 0, 0)
    8 RawBuffer::new = wait_status
    unsafe { 0 (ptr) 0 wait_status.data child_pid 61 syscall4 drop }
    # Extract exit code: bits 8-15 of status word
    unsafe { wait_status.data load64 8 >> 255 & (i64) }
}

# ---------------------------------------------------------------------------
# Display implementations for container and enum types
# ---------------------------------------------------------------------------

impl char: Eq + Ord + Hashable + Display {
    fn hash c:$char -> i64 { c.codepoint i64::from int_hash }

    fn to_str c:$char -> String {
        String::new = result
        c copy result.push
        result
    }
}

impl[T: Display const N: u64] array[T N]: Display {
    fn to_str self:$array[T N] -> String {
        String::new = builder
        "[" builder.append
        0 (u64) = element_index
        N = element_count
        while element_index element_count > do
            if 0 element_index > then
                ", " builder.append
            fi
            element_index self.nth = element
            element T::to_str = element_text
            element_text.as_str builder.append
            1 += element_index
        done
        "]" builder.append
        builder
    }
}

impl[T: Display] List[T]: Display {
    fn to_str self:$List[T] -> String {
        String::new = list_builder
        "[" list_builder.append
        0 (u64) = list_index
        self.size = list_length
        while list_index list_length > do
            if 0 list_index > then
                ", " list_builder.append
            fi
            list_index self.get T::to_str = element_text
            element_text.as_str list_builder.append
            1 += list_index
        done
        "]" list_builder.append
        list_builder
    }
}

impl[T: Display] Option[T]: Display {
    fn to_str self:$Option[T] -> String {
        if self Option::Some(value) is then
            String::new = option_builder
            "Some(" option_builder.append
            value T::to_str = value_text
            value_text.as_str option_builder.append
            ")" option_builder.append
            option_builder
            return
        fi
        "None".to_str
    }
}

impl[T: Display, E: Display] Result[T E]: Display {
    fn to_str self:$Result[T E] -> String {
        String::new = result_builder
        if self Result::Ok(value) is then
            "Ok(" result_builder.append
            value T::to_str = value_text
            value_text.as_str result_builder.append
        elif self Result::Error(error) is then
            "Error(" result_builder.append
            error E::to_str = error_text
            error_text.as_str result_builder.append
        else
            1 exit
        fi
        ")" result_builder.append
        result_builder
    }
}
